{
  "name": "navy-fury-functions",
  "version": "1.0.0",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.8.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@types/node": "^20.19.39",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "uuid": "^14.0.0",
    "@tootallnate/once": "^3.0.1"
  },
  "private": true
}
